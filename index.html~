<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="styles.css"/>
    <link href="https://fonts.googleapis.com/css?family=Montserrat:400,700" rel="stylesheet">
    <title>Ron Machine</title>
  </head>
  <body>
    <div id="app">
      <center><h1>Ron Machine</h1></center>
      <div v-if="loading">
			<p>Loading...</p>
      </div>
      <div v-else>
     		<center><img src="ron-swanson.jpg" alt="The Ron"></center>
      	<p>
				<center><button v-on:click="randomComic">Give Me Wisdom</button></center>
      	</p>
      	
			<p><i><center>{{joke}}</center></i></p>
			<h3>Add a Comment</h3>
			<form v-on:submit.prevent="addComment">
	  			<input v-model="addedName" placeholder="Name"></p>
	  			<textarea v-model="addedComment"></textarea>
	  			<br/>
	  			<button type="submit">Comment</button>
			</form>
			<h3>Comments</h3>
			<div v-for="comment in comments[number]">
	  			<hr>
	  			<p>{{comment.text}}</p>
	  			<p><i>-- {{comment.author}}</i></p>
	  			<p>{{comment.time}}</p>
			</div>
      </div>


    </div>
    
    <footer>
    <br>
    <br>
    <br>
    <center>*Jason's <a href="https://github.com/BYU-CS260-Winter-2018/lab-3-jasontd92">Github</a>*</center>
    <script src="https://unpkg.com/vue-star-rating/dist/star-rating.min.js"></script>			
    <script src="https://cdn.jsdelivr.net/npm/vue@2.5.13/dist/vue.js"></script>
    <script src="script.js"></script>
  </body>
</html>
